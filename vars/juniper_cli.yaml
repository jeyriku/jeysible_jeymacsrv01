---
# Juniper specific variables
connection: ansible.netcommon.network_cli
ansible_user: "{{ device_username }}"
ansible_password: "{{ device_password }}"
ansible_network_os: junipernetworks.junos.junos
